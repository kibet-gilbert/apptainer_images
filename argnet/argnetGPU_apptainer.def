BootStrap: docker
From: mambaorg/micromamba:1.5.10-noble

%files
    conda_GPU.yaml /scratch/conda.yml

%post
    micromamba shell init -s bash -p /opt/conda  # ensure mamba works
    micromamba install -y -n base -f /scratch/conda.yml
    micromamba install -y -n base conda-forge::procps-ng
    #micromamba run -n base pip install git+https://github.com/id-bioinfo/ARGnet.git
    micromamba run -n base git clone https://github.com/id-bioinfo/ARGnet.git /opt/argnet
    micromamba env export -n base --explicit > environment.lock
    echo ">> CONDA_LOCK_START"
    cat environment.lock
    echo "<< CONDA_LOCK_END"
    rm -rf /root/.cache /opt/conda/pkgs /tmp/*
    micromamba clean -a -y

%environment
    export PATH="$MAMBA_ROOT_PREFIX/bin:$PATH"
    export PATH="/opt/conda/bin:$PATH"
    export PYTHONPATH="/opt/argnet/scripts:$PYTHONPATH"
    export CONDA_PREFIX="/opt/conda"
    # any other env vars your workflow needs...

